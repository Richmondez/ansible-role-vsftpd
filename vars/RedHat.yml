# roles/vsftpd/vars/RedHat.yml
---

vsftpd_packages:
  - vsftpd
  - "{{ 'libsemanage-python' if ansible_distribution_major_version < 8 else 'policycoreutils-python-utils' }}"

vsftpd_service: vsftpd

vsftpd_config_file: /etc/vsftpd/vsftpd.conf
